package main

var result []string

var oneRes string

func dfs(start int, commaLen int, str string) {

	for i := start; i < len(str); i++ {
	}
}
func restoreIpAddresses(s string) []string {
	stk := new([]int)
	return result
}
